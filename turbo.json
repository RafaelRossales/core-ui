{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "dev": {
      "cache": false
    },
    "build": {
      "cache": true,
      "outputs": ["/dist/**", "storybook-static/**"],
      "dependsOn": ["^build"]
    }
  }
}
